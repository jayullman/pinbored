import React from 'react';

// import '../styles/home.css';

const Home = () =>
  (
    <div>
      <p>This is the home page</p>
      <p>Welcome. To view pins posted by all views, visit the All Pins section.</p>
      <p>You must first login through twitter before you can post or like any pins.</p>
    </div>
  );

export default Home;
